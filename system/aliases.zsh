ialias ping='ping -c 5'
ialias l='ls -lh'
ialias l.='ls -ld .*'
ialias ll='ls -lAh'
ialias ls='ls --color=tty'


# expandable aliases
ealias clh='curl localhost:'

# global aliases
alias -g L='| less'
alias -g G='| grep'

# suffix aliases
alias -s {zip,ZIP}='unzip'

